version: "3.8"

services:
  bossqu-ai:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bossqu-ai
    environment:
      - CHROMA_PERSIST_DIRECTORY=/store
      - PORT=7788
    ports:
      - "7788:7788"
    volumes:
      - ./store:/store
